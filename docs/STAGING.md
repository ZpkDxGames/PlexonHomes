# Staging

Before disabling existing home commands: back up Essentials userdata and `homes.db`, scan and plan migration, execute against staging data, verify player counts/names/worlds, exercise set/get/rename/delete, test movement/damage cancellation and unsafe destinations, and retain a rollback path.
