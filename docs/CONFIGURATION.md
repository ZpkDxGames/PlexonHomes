# Configuration

`config.yml` controls canonical home naming, default and permission limits, world restrictions, warmup/cooldown/cancellation behavior, optional teleport fee, and bounded safe-location fallback. Runtime reload replaces the immutable config snapshot atomically for new operations.
